<div class="first-row">
	<%= link_to('progress', "/", class:"brand pull-left") %></div>
	

	<div class="login-cta pull-right">
	
	<% if user_signed_in? %>
	  <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>        
	 <% else %>
	  <%= link_to('Login', new_user_session_path)  %>  
	  
	<% end %>

	</div>

</div>